language: python
python:
  - '3.5'
  - '3.6'
  - '3.7'
  - '3.8'
install:
  - pip install -r requirements.txt
  - install coveralls
script: pytest --cov=sandbox --cov-report term-missing
after_success:
  - coveralls
